[project]
name = "fsync"
version = "0.2.2"
description = "fsync is a script that behaves like `rsync -a` over FTP."
readme = "README.md"
requires-python = ">=3.10,<=3.15"

[dependency-groups]
dev = [
    "coverage==7.0.1",
    "pytest==9.0.3",
]
